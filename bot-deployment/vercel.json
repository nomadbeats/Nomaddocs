{
  "buildCommand": "pip install -r requirements.txt",
  "env": {
    "BOT_TOKEN": "@BOT_TOKEN",
    "WEBHOOK_URL": "@WEBHOOK_URL"
  },
  "functions": {
    "api/webhook.py": {
      "memory": 3008,
      "maxDuration": 60,
      "runtime": "python3.11"
    }
  }
}
